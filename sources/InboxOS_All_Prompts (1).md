

InboxOS — Complete AI Coding Agent Prompts
All 65 Prompts (5 per contributor × 13 contributors)
## PROFESSIONAL TEAM PROMPTS
## P1 — Lead Core Architect
Prompt 1: Initialize Project & DB [Hard]
**System Prompt:** You are an expert Lead Core Architect building InboxOS, a next-generation open-source
AI email operating system.↪
**Stack:** Node.js, TypeScript, PostgreSQL, Prisma ORM
**Task:** Set up the backend project and initial database schema.
**Step-by-Step Instructions:**
- Initialize a new Node.js project with TypeScript in `./backend`.
- Install Prisma CLI and initialize it with PostgreSQL.
- Define the following models in `schema.prisma`: User (id, email, passwordHash, createdAt), Email (id,
sender, recipient, subject, body, threadId, status, createdAt), Thread (id, summary, createdAt).↪
- Create a migration script `npm run migrate:dev`.
**Design/Architecture Constraints:**
- Use strict TypeScript settings (`strict: true`).
- Ensure database relations are correctly mapped (1 User -&gt; Many Emails, 1 Thread -&gt; Many Emails).
**Acceptance Criteria:**
The project builds without TypeScript errors, and running the migration successfully creates the tables
in a local Postgres instance.↪
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 2: Implement User Authentication [Hard]
**System Prompt:** You are an expert Lead Core Architect building InboxOS, a next-generation open-source
AI email operating system.↪
**Stack:** Node.js, Express, JWT, bcrypt, Prisma
**Task:** Build the authentication REST API service.
**Step-by-Step Instructions:**
- Setup an Express server in `./backend/src/server.ts`.
- Create an `AuthService` class that handles password hashing using `bcrypt` and JWT token generation.
- Implement POST `/api/auth/register` (creates user in DB) and POST `/api/auth/login` (returns JWT).
- Create a middleware `requireAuth` that validates the JWT from an HTTP-only cookie.
**Design/Architecture Constraints:**
Generated by Kimi.ai

- Passwords must be hashed with a minimum of 10 salt rounds.
- The JWT must be stored in a secure, HTTP-only cookie, NOT returned in the JSON body.
**Acceptance Criteria:**
A user can successfully register and login, receiving a valid HTTP-only cookie containing the JWT.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 3: Set up Core Email Ingestion [Medium]
**System Prompt:** You are an expert Lead Core Architect building InboxOS, a next-generation open-source
AI email operating system.↪
**Stack:** Node.js, Express, Prisma
**Task:** Build the core webhook endpoint that ingests incoming emails.
**Step-by-Step Instructions:**
- Create an endpoint POST `/api/webhooks/incoming`.
- Validate that the incoming JSON payload contains `sender`, `recipient`, `subject`, `body`, and
`messageId`.↪
- Check if the `In-Reply-To` header exists to link it to an existing Thread. If not, create a new
## Thread.↪
- Save the Email to the PostgreSQL database via Prisma.
**Design/Architecture Constraints:**
- Payload validation must be strict using a library like `zod`.
- The endpoint must return a 201 Created immediately after saving, without blocking for AI processing.
**Acceptance Criteria:**
Sending a mock JSON email payload to the endpoint successfully creates a Thread and Email record in the
## DB.↪
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 4: Create Internal Event Bus [Medium]
**System Prompt:** You are an expert Lead Core Architect building InboxOS, a next-generation open-source
AI email operating system.↪
**Stack:** Node.js, Redis (ioredis), EventEmitter
**Task:** Implement a pub/sub mechanism to decouple email ingestion from AI processing.
**Step-by-Step Instructions:**
- Install `ioredis` and set up a Redis client.
- Create an `EventBus` service with `publish(topic, payload)` and `subscribe(topic, handler)` methods.
Generated by Kimi.ai

- Modify the webhook endpoint to call `EventBus.publish('email.received', { emailId })` after saving to
the DB.↪
- Create a placeholder worker script `worker.ts` that subscribes to 'email.received' and logs the ID.
**Design/Architecture Constraints:**
- The event bus must handle connection errors gracefully.
- Use a structured JSON format for all payloads published to Redis.
**Acceptance Criteria:**
Ingesting an email via the webhook successfully triggers a console log in the worker script.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 5: Develop User Settings API [Easy]
**System Prompt:** You are an expert Lead Core Architect building InboxOS, a next-generation open-source
AI email operating system.↪
**Stack:** Node.js, Express, Prisma
**Task:** Build CRUD endpoints for user preferences.
**Step-by-Step Instructions:**
- Update Prisma schema to add a `UserSettings` model (theme, signature, autoReply) with a 1-to-1
relation to User.↪
- Create GET `/api/users/me/settings` to fetch preferences.
- Create PUT `/api/users/me/settings` to update preferences.
- Ensure these routes use the `requireAuth` middleware.
**Design/Architecture Constraints:**
- If a user does not have settings created yet, the GET endpoint should return default values.
- Validate the PUT payload using `zod`.
**Acceptance Criteria:**
An authenticated user can successfully fetch and update their settings via the API.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
P2 — AI Engine Lead
Prompt 1: Integrate OpenAI SDK [Hard]
**System Prompt:** You are an expert AI Engine Lead building InboxOS, a next-generation open-source AI
email operating system.↪
**Stack:** Node.js, OpenAI SDK, TypeScript
**Task:** Create the core AI service wrapper for classification.
Generated by Kimi.ai

**Step-by-Step Instructions:**
- Install `openai` package and setup `AIService` class.
- Implement `classifyEmail(subject, body)`.
- Use the OpenAI chat completions API with the 'gpt-4o-mini' model.
- Use Structured Outputs (JSON Schema) to force the model to return exactly: `{ &quot;category&quot;:
&quot;string (urgent|newsletter|personal|work|spam)&quot;, &quot;confidence&quot;: number }`.↪
**Design/Architecture Constraints:**
- API Keys must be loaded from `.env` and never hardcoded.
- Handle rate limit errors (429) using an exponential backoff retry mechanism.
**Acceptance Criteria:**
Calling `classifyEmail` with a sample email returns a strictly typed JSON object with a valid category.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 2: Build the AI Worker [Hard]
**System Prompt:** You are an expert AI Engine Lead building InboxOS, a next-generation open-source AI
email operating system.↪
**Stack:** Node.js, Redis, Prisma
**Task:** Connect the AI service to the core event bus to process emails asynchronously.
**Step-by-Step Instructions:**
- Update the `worker.ts` script to subscribe to 'email.received'.
- When an event is received, fetch the Email from Prisma using the provided ID.
- Pass the email subject and body to `AIService.classifyEmail()`.
- Update the Email record in the database with the resulting category.
**Design/Architecture Constraints:**
- The worker must run continuously in the background.
- If the AI API fails, the worker should log the error and mark the email status as 'failed'.
**Acceptance Criteria:**
When an email is ingested by the webhook, the worker automatically classifies it and updates the
database without manual intervention.↪
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 3: Implement Summary Generation [Medium]
**System Prompt:** You are an expert AI Engine Lead building InboxOS, a next-generation open-source AI
email operating system.↪
Generated by Kimi.ai

**Stack:** Node.js, OpenAI SDK, Prisma
**Task:** Generate short executive summaries for email threads.
**Step-by-Step Instructions:**
- Add a `generateSummary(threadId)` method to `AIService`.
- Fetch all Emails belonging to the thread ordered by date.
- Concatenate the bodies into a single string.
- Prompt the LLM: 'Summarize the following email thread in 2-3 sentences. Focus on the main outcome or
required action.'↪
- Save the summary to the Thread model.
**Design/Architecture Constraints:**
- Truncate the concatenated string to a maximum of 8000 tokens to avoid exceeding context limits.
- The summary should be objective and concise.
**Acceptance Criteria:**
Calling the summary function correctly updates the Thread record with a 2-3 sentence summary.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 4: Develop Action Extraction [Medium]
**System Prompt:** You are an expert AI Engine Lead building InboxOS, a next-generation open-source AI
email operating system.↪
**Stack:** Node.js, OpenAI SDK, Prisma
**Task:** Extract actionable tasks directly from email bodies.
**Step-by-Step Instructions:**
- Update the Prisma schema to add an `ActionItem` model (id, emailId, taskDescription, isCompleted).
- Update the `classifyEmail` system prompt to also return an array of `actions` in the JSON structured
output.↪
- Modify the worker to iterate over the extracted actions and save them to the database using
`prisma.actionItem.createMany`.↪
**Design/Architecture Constraints:**
- The LLM should only extract explicit, concrete tasks (e.g., 'Send the report by Friday').
- Handle the case where the actions array is empty.
**Acceptance Criteria:**
Processing an email containing a request (e.g., 'Please review the attached PDF') results in a new
ActionItem in the database.↪
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Generated by Kimi.ai

Prompt 5: Create RAG Context Builder [Hard]
**System Prompt:** You are an expert AI Engine Lead building InboxOS, a next-generation open-source AI
email operating system.↪
**Stack:** Node.js, pgvector, OpenAI API
**Task:** Set up a vector database pipeline to retrieve relevant past emails.
**Step-by-Step Instructions:**
- Ensure the PostgreSQL database has the `pgvector` extension enabled.
- Update the Prisma schema to support vector embeddings (you may need to use raw SQL for the vector
column).↪
- Create a function `embedEmail(emailId)` that generates a text embedding using
'text-embedding-3-small' and saves it to the DB.↪
- Create a function `searchSimilarEmails(query, limit=5)` that calculates cosine similarity and
returns the closest matches.↪
**Design/Architecture Constraints:**
- Text must be cleaned (HTML tags removed) before embedding.
- Vector dimension must match the chosen embedding model (e.g., 1536).
**Acceptance Criteria:**
Searching for 'invoice' returns past emails containing similar semantic meaning, ranked by similarity.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
## P3 — Frontend Architect
Prompt 1: Scaffold React App [Medium]
**System Prompt:** You are an expert Frontend Architect building InboxOS, a next-generation open-source
AI email operating system.↪
**Stack:** React 18, Vite, TypeScript, TailwindCSS
**Task:** Initialize the foundation of the InboxOS web dashboard.
**Step-by-Step Instructions:**
- Run `npm create vite@latest frontend -- --template react-ts`.
- Install `tailwindcss`, `postcss`, and `autoprefixer`. Initialize the config.
- Update `tailwind.config.js` to include our custom color palette: `bg-base: #080b14`, `bg-elevated:
## #111827`, `accent: #6366f1`.↪
- Create a foundational Layout component with a left Sidebar (width 260px) and a main content area.
- Ensure global styles use 'Inter' font and dark mode defaults.
**Design/Architecture Constraints:**
- Do not use generic colors. Strictly adhere to the dark glassmorphism design system variables.
- Components must use functional components and hooks.
**Acceptance Criteria:**
The React app runs locally without errors, displaying a dark-themed layout with a visible sidebar.
Generated by Kimi.ai

**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 2: Build Authentication Views [Medium]
**System Prompt:** You are an expert Frontend Architect building InboxOS, a next-generation open-source
AI email operating system.↪
**Stack:** React, React Router, TailwindCSS
**Task:** Create the Login and Registration screens.
**Step-by-Step Instructions:**
- Set up routing using `react-router-dom`.
- Create a `/login` route rendering a LoginForm component (email, password).
- Create a `/register` route rendering a RegisterForm component.
- Style the forms with glassmorphism effects (backdrop-blur, translucent borders).
- Create an `AuthContext` to manage the global authentication state (isAuthenticated, user object).
**Design/Architecture Constraints:**
- Form inputs must have hover, focus, and error states visually defined.
- The context should expose `login` and `logout` functions.
**Acceptance Criteria:**
Users can navigate between the login and register pages, and the forms look polished and responsive.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 3: Develop Inbox List View [Hard]
**System Prompt:** You are an expert Frontend Architect building InboxOS, a next-generation open-source
AI email operating system.↪
**Stack:** React, React Query (TanStack), TailwindCSS
**Task:** Build the primary interface where users view their emails.
**Step-by-Step Instructions:**
- Install `@tanstack/react-query` to handle data fetching.
- Create an `EmailList` component that fetches data from `/api/emails`.
- Map over the data to render `EmailRow` components showing Sender, Subject, Date, and AI Category
badges.↪
- Implement basic pagination or infinite scrolling.
- Use a skeleton loader while the data is fetching.
**Design/Architecture Constraints:**
- Unread emails should have a bolder font weight and a distinct indicator dot.
Generated by Kimi.ai

- Category badges must be color-coded (e.g., red for Urgent, gray for Newsletter).
**Acceptance Criteria:**
The inbox successfully renders a list of emails fetched from the API with proper loading states.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 4: Implement Email Detail View [Medium]
**System Prompt:** You are an expert Frontend Architect building InboxOS, a next-generation open-source
AI email operating system.↪
**Stack:** React, DOMPurify, TailwindCSS
**Task:** Create the component to display a selected email and its AI insights.
**Step-by-Step Instructions:**
- Create an `EmailViewer` component that takes an `emailId` prop.
- Fetch the full email thread and body. Sanitize the HTML body using `dompurify` before rendering it
using `dangerouslySetInnerHTML`.↪
- Create an AI Sidebar Panel on the right side of the viewer.
- Display the AI-generated Thread Summary and the list of extracted Action Items in this panel.
**Design/Architecture Constraints:**
- Under no circumstances should unsanitized HTML be rendered (XSS risk).
- The AI panel should have a distinct, slightly elevated background color to separate it from the email
content.↪
**Acceptance Criteria:**
Clicking an email in the list view correctly renders the safe HTML body and displays the AI summary
sidebar.↪
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 5: Build Compose Modal [Medium]
**System Prompt:** You are an expert Frontend Architect building InboxOS, a next-generation open-source
AI email operating system.↪
**Stack:** React, TailwindCSS, React Hook Form
**Task:** Implement the UI for writing and replying to emails.
**Step-by-Step Instructions:**
- Create a global `ComposeModal` component that can be triggered from anywhere via context.
- Use `react-hook-form` to manage the form state (To, CC, Subject, Body).
- Implement a basic rich text editor (or a highly styled textarea for MVP).
- Add an 'AI Draft Reply' button that, when clicked, shows a loading state.
Generated by Kimi.ai

- Implement the submit handler to POST the data to `/api/emails/send`.
**Design/Architecture Constraints:**
- The modal must have an overlay with `backdrop-blur-sm` and handle click-outside to close.
- Ensure form validation (e.g., valid 'To' email addresses).
**Acceptance Criteria:**
The modal opens correctly, validates input, and handles submission gracefully with a success toast
notification.↪
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
## P4 — Integration Specialist
Prompt 1: Set up Gmail OAuth Flow [Hard]
**System Prompt:** You are an expert Integration Specialist building InboxOS, a next-generation
open-source AI email operating system.↪
**Stack:** Node.js, Express, googleapis
**Task:** Implement the OAuth 2.0 flow to connect user Gmail accounts.
**Step-by-Step Instructions:**
- Install the `googleapis` package.
- Create an endpoint GET `/api/integrations/gmail/auth` that generates the Google OAuth URL requesting
`https://mail.google.com/` scope.↪
- Create the callback endpoint GET `/api/integrations/gmail/callback`.
- Exchange the authorization code for access/refresh tokens and save them to a new `Integration` model
in Prisma securely.↪
**Design/Architecture Constraints:**
- The redirect URI must match the configuration in the Google Cloud Console.
- Refresh tokens must be encrypted before being stored in the database.
**Acceptance Criteria:**
A user can initiate the flow, approve access on Google, and the tokens are successfully saved to their
account.↪
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 2: Implement Gmail Sync Worker [Hard]
**System Prompt:** You are an expert Integration Specialist building InboxOS, a next-generation
open-source AI email operating system.↪
**Stack:** Node.js, googleapis, Prisma
Generated by Kimi.ai

**Task:** Build a worker to pull emails from the Gmail API.
**Step-by-Step Instructions:**
- Create a `GmailSyncService` class.
- Implement a method `syncLatestEmails(userId)` that retrieves the user's tokens from the DB and
instantiates an OAuth2 client.↪
- Fetch the latest 50 message IDs from Gmail using `gmail.users.messages.list`.
- Fetch the full payload for each message, map the headers (From, To, Subject) and body to our InboxOS
`Email` schema, and save it to the DB.↪
**Design/Architecture Constraints:**
- Handle the base64url decoding required for Gmail message bodies.
- Store the `historyId` or `lastMessageId` to prevent fetching duplicates on subsequent syncs.
**Acceptance Criteria:**
Running the sync function successfully imports recent emails from a connected Gmail account into the
local database.↪
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 3: Build IMAP Connector [Hard]
**System Prompt:** You are an expert Integration Specialist building InboxOS, a next-generation
open-source AI email operating system.↪
**Stack:** Node.js, node-imap, mailparser
**Task:** Implement generic IMAP support for non-Gmail providers.
**Step-by-Step Instructions:**
- Install `imap` and `mailparser` packages.
- Create an `IMAPService` class that accepts connection credentials (host, port, user, pass).
- Implement a `connect()` method that opens a connection via TLS.
- Implement an `idle()` method that listens for new emails in the INBOX. Upon receiving one, fetch the
raw `.eml`, parse it, and save it to the database.↪
**Design/Architecture Constraints:**
- Connections must strictly use TLS/SSL.
- Ensure the connection automatically reconnects if the socket is closed or times out.
**Acceptance Criteria:**
The service successfully connects to an IMAP server, listens for new messages, and parses them
accurately.↪
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Generated by Kimi.ai

Prompt 4: Create Outbound SMTP Service [Medium]
**System Prompt:** You are an expert Integration Specialist building InboxOS, a next-generation
open-source AI email operating system.↪
**Stack:** Node.js, nodemailer
**Task:** Implement the email sending capability.
**Step-by-Step Instructions:**
## 1. Install `nodemailer`.
- Create an endpoint POST `/api/emails/send`.
- Create an `EmailSenderService` that determines the user's connection type.
- If the user uses IMAP/SMTP, instantiate a nodemailer transport using their saved credentials and send
the email.↪
- If successful, save a copy of the sent email to the database under the correct Thread.
**Design/Architecture Constraints:**
- Passwords used for SMTP must be decrypted in memory and never logged.
- Ensure correct MIME formatting for HTML emails.
**Acceptance Criteria:**
Sending a payload to the endpoint successfully delivers the email to the recipient's actual inbox.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 5: Webhook Management API [Easy]
**System Prompt:** You are an expert Integration Specialist building InboxOS, a next-generation
open-source AI email operating system.↪
**Stack:** Node.js, Express, Prisma
**Task:** Allow users to register their own webhooks.
**Step-by-Step Instructions:**
- Update the Prisma schema with a `WebhookEndpoint` model (id, userId, targetUrl, events[]).
- Create CRUD endpoints for Webhooks under `/api/webhooks/config`.
- Update the internal Event Bus to check for registered external webhooks whenever an 'email.received'
or 'action.created' event fires.↪
- Write a utility that makes HTTP POST requests to the user's `targetUrl` with the event payload.
**Design/Architecture Constraints:**
- Outbound webhook requests must have a strict timeout (e.g., 5 seconds) to prevent hanging the system.
- Sign the outbound payload with a secret so users can verify it came from InboxOS.
**Acceptance Criteria:**
A user can register a webhook URL, and it automatically receives HTTP POST requests when new emails
arrive.↪
Generated by Kimi.ai

**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
P5 — Infrastructure/DevOps
Prompt 1: Containerize the Application [Medium]
**System Prompt:** You are an expert Infrastructure/DevOps building InboxOS, a next-generation
open-source AI email operating system.↪
**Stack:** Docker, Docker Compose
**Task:** Create the local development container architecture.
**Step-by-Step Instructions:**
- Write a `Dockerfile` for the Node.js backend using the `node:20-alpine` image.
- Write a multi-stage `Dockerfile` for the React frontend, serving the build output via Nginx.
- Create a `docker-compose.yml` file at the root.
- Define services for `backend`, `frontend`, `postgres` (with initial DB setup script), and `redis`.
- Ensure proper networking and environment variable mapping.
**Design/Architecture Constraints:**
- Containers must run as non-root users for security.
- Use bind mounts in docker-compose so code changes reflect immediately during development.
**Acceptance Criteria:**
Running `docker-compose up` starts all services successfully, and the dashboard is accessible on
localhost:80.↪
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 2: Set up GitHub Actions CI [Medium]
**System Prompt:** You are an expert Infrastructure/DevOps building InboxOS, a next-generation
open-source AI email operating system.↪
**Stack:** GitHub Actions, YAML
**Task:** Implement the Continuous Integration pipeline.
**Step-by-Step Instructions:**
## 1. Create `.github/workflows/ci.yml`.
- Define a job that runs on `push` to `main` and `pull_request`.
- The job should checkout the code, setup Node.js v20, and cache `node_modules`.
- Execute `npm run lint`, `npm run typecheck`, and `npm run test` for both the frontend and backend
directories.↪
- Fail the build immediately if any step fails.
**Design/Architecture Constraints:**
Generated by Kimi.ai

- Optimize action run times by properly caching npm dependencies.
- Ensure database-dependent tests are skipped or run against a temporary postgres service container.
**Acceptance Criteria:**
Pushing a commit with a TypeScript error successfully fails the GitHub Actions workflow.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 3: Configure Production Database [Hard]
**System Prompt:** You are an expert Infrastructure/DevOps building InboxOS, a next-generation
open-source AI email operating system.↪
**Stack:** Terraform, AWS RDS
**Task:** Write Infrastructure as Code to provision the database layer.
**Step-by-Step Instructions:**
- Create a `terraform/` directory.
- Write AWS provider configuration.
- Define an `aws_db_instance` resource for PostgreSQL 16.
- Define an `aws_elasticache_cluster` resource for Redis.
- Create necessary VPC, Subnet Groups, and Security Groups. Ensure the DB is in a private subnet and
only accessible from the application security group.↪
**Design/Architecture Constraints:**
- Credentials must not be hardcoded in the `.tf` files (use variables).
- Multi-AZ deployment should be enabled for high availability.
**Acceptance Criteria:**
Running `terraform plan` outputs a clean, secure infrastructure deployment blueprint without exposing
the databases to the public internet.↪
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 4: Implement Rate Limiting [Medium]
**System Prompt:** You are an expert Infrastructure/DevOps building InboxOS, a next-generation
open-source AI email operating system.↪
**Stack:** Node.js, Express, Redis
**Task:** Protect the APIs from abuse.
**Step-by-Step Instructions:**
- Install `express-rate-limit` and `rate-limit-redis`.
- Create a global middleware for all `/api` routes.
- Configure it to use the Redis instance as the store.
Generated by Kimi.ai

- Set limits: 100 requests per 15 minutes for public endpoints (like login), and 1000 requests per 15
minutes for authenticated user routes (tracked by JWT user ID).↪
**Design/Architecture Constraints:**
- The API must return a 429 Too Many Requests status when the limit is exceeded.
- Include standard `X-RateLimit-*` headers in the responses.
**Acceptance Criteria:**
Spamming the API with 150 rapid requests successfully triggers a 429 response block.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 5: Set up Monitoring and Logging [Easy]
**System Prompt:** You are an expert Infrastructure/DevOps building InboxOS, a next-generation
open-source AI email operating system.↪
**Stack:** Node.js, Winston, Prometheus
**Task:** Implement structured logging and metrics collection.
**Step-by-Step Instructions:**
- Install and configure `winston` for the backend. Configure it to output JSON structured logs to
stdout.↪
## 2. Install `prom-client`.
- Create a GET `/metrics` endpoint that exposes default Node.js metrics (CPU, memory, event loop lag).
- Add a custom metric counter for `emails_processed_total` and increment it in the webhook/worker.
**Design/Architecture Constraints:**
- Do not log sensitive user data (passwords, email bodies) in plaintext.
- Ensure the `/metrics` endpoint is lightweight and secure.
**Acceptance Criteria:**
Accessing `/metrics` returns Prometheus-formatted text, and logs appear as valid JSON strings in the
console.↪
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Generated by Kimi.ai

## BEGINNER TEAM PROMPTS
## B1 — Beginner Backend
Prompt 1: Create Health Check [Easy]
**System Prompt:** You are an expert Backend Developer building InboxOS, a next-generation open-source
AI email operating system.↪
**Stack:** Node.js, Express, Jest
**Task:** Implement API Health Check
**Step-by-Step Instructions:**
- Create GET /api/health.
- Return {status: 'ok', timestamp: new Date()}.
- Write a Jest unit test using Supertest.
**Design/Architecture Constraints:**
- Keep it lightweight.
- Do not connect to the database for this check.
**Acceptance Criteria:**
Endpoint returns 200 OK and the test passes.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 2: User Profile API [Easy]
**System Prompt:** You are an expert Backend Developer building InboxOS, a next-generation open-source
AI email operating system.↪
**Stack:** Node.js, Prisma, JWT
**Task:** Fetch authenticated user details
**Step-by-Step Instructions:**
- Create GET /api/users/profile.
- Use the requireAuth middleware.
- Query the user by ID from Prisma.
- Exclude the password hash from the response.
**Design/Architecture Constraints:**
- Return 404 if user is deleted.
- Use the existing user model.
**Acceptance Criteria:**
Endpoint returns a valid user JSON object without the password.
Generated by Kimi.ai

**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 3: Email Search API [Medium]
**System Prompt:** You are an expert Backend Developer building InboxOS, a next-generation open-source
AI email operating system.↪
**Stack:** Node.js, Prisma
**Task:** Implement basic email search
**Step-by-Step Instructions:**
- Create GET /api/emails/search?q=term.
- Query Prisma where Subject or Body contains the term.
- Implement pagination (limit/offset).
**Design/Architecture Constraints:**
- Use Prisma's `contains` filter.
- Limit results to 20 per page.
**Acceptance Criteria:**
Searching returns a paginated list of matching emails.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 4: Request Validation [Easy]
**System Prompt:** You are an expert Backend Developer building InboxOS, a next-generation open-source
AI email operating system.↪
**Stack:** Node.js, Zod
**Task:** Validate incoming API requests
**Step-by-Step Instructions:**
## 1. Install `zod`.
- Create a validation schema for the Registration endpoint (email must be valid, password &gt;= 8
chars).↪
- Create an Express middleware to run the validation.
**Design/Architecture Constraints:**
- Return 400 Bad Request with a clear error array if validation fails.
**Acceptance Criteria:**
Sending a bad email format to /register returns a 400 error.
Generated by Kimi.ai

**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 5: Folder Management API [Easy]
**System Prompt:** You are an expert Backend Developer building InboxOS, a next-generation open-source
AI email operating system.↪
**Stack:** Node.js, Prisma
**Task:** Build CRUD for user folders
**Step-by-Step Instructions:**
- Add a Folder model to Prisma.
- Create POST /api/folders to create a folder.
- Create GET /api/folders to list them.
- Create DELETE /api/folders/:id.
**Design/Architecture Constraints:**
- A folder belongs to a User.
- Cannot delete default system folders (Inbox, Sent).
**Acceptance Criteria:**
Users can create and delete custom folders via the API.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
## B2 — Beginner Frontend
Prompt 1: Button Component [Easy]
**System Prompt:** You are an expert Frontend Developer building InboxOS, a next-generation open-source
AI email operating system.↪
**Stack:** React, TailwindCSS
**Task:** Build a reusable Button
**Step-by-Step Instructions:**
- Create `Button.tsx`.
- Accept props: variant (primary/secondary), size, isLoading.
- Style using Tailwind classes.
- Show a spinner SVG if isLoading is true.
**Design/Architecture Constraints:**
- Use the CSS variables for colors (`var(--accent)`).
- Disable the button when loading.
**Acceptance Criteria:**
Generated by Kimi.ai

Button renders correctly in all variations and states.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 2: Avatar Component [Easy]
**System Prompt:** You are an expert Frontend Developer building InboxOS, a next-generation open-source
AI email operating system.↪
**Stack:** React, TailwindCSS
**Task:** Build an Avatar display
**Step-by-Step Instructions:**
- Create `Avatar.tsx`.
- Take props: name, imageUrl.
- If imageUrl is valid, display it.
- Fallback: extract initials from the name and display them on a colored background.
**Design/Architecture Constraints:**
- Image must be a perfect circle.
- Use `onError` to handle broken image links.
**Acceptance Criteria:**
Avatar shows an image, or falls back to initials cleanly.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 3: Empty State UI [Easy]
**System Prompt:** You are an expert Frontend Developer building InboxOS, a next-generation open-source
AI email operating system.↪
**Stack:** React, Lucide React
**Task:** Design empty state illustrations
**Step-by-Step Instructions:**
- Create `EmptyState.tsx`.
- Take props: title, description, icon (from lucide-react).
- Center the content vertically and horizontally with a muted text color.
**Design/Architecture Constraints:**
- Use `var(--text-muted)` for the description.
- The icon should be large and semi-transparent.
**Acceptance Criteria:**
Generated by Kimi.ai

Navigating to an empty folder displays the empty state visually.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 4: Sidebar Navigation [Medium]
**System Prompt:** You are an expert Frontend Developer building InboxOS, a next-generation open-source
AI email operating system.↪
**Stack:** React Router, TailwindCSS
**Task:** Build the layout sidebar
**Step-by-Step Instructions:**
- Create `SidebarNav.tsx`.
- Map over an array of navigation items.
- Use React Router's `useLocation` to determine the active link.
- Apply active styling (e.g., a background highlight).
**Design/Architecture Constraints:**
- Sidebar must be 260px wide on desktop.
- Active state must be clearly distinct from inactive.
**Acceptance Criteria:**
Clicking links updates the active state and navigates without full page reload.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 5: Settings Modal [Medium]
**System Prompt:** You are an expert Frontend Developer building InboxOS, a next-generation open-source
AI email operating system.↪
**Stack:** React, TailwindCSS
**Task:** Build a generic Modal shell
**Step-by-Step Instructions:**
- Create `Modal.tsx`.
- Use a fixed overlay with `backdrop-blur`.
- Center the modal dialog box.
- Add a close (X) button in the top right.
- Render `children` inside the content area.
**Design/Architecture Constraints:**
- Modal must close when clicking the overlay backdrop.
- Trap focus inside the modal for accessibility.
Generated by Kimi.ai

**Acceptance Criteria:**
Modal opens, displays content, and closes on backdrop click.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
B3 — Beginner AI/Data
Prompt 1: Synthetic Email Gen [Easy]
**System Prompt:** You are an expert AI/Data Dev building InboxOS, a next-generation open-source AI
email operating system.↪
**Stack:** Python, Faker
**Task:** Generate mock test data
**Step-by-Step Instructions:**
- Write a Python script `generate_mock_data.py`.
- Use the `Faker` library.
- Generate 100 JSON objects containing a realistic email structure.
- Save output to `mock_emails.json`.
**Design/Architecture Constraints:**
- Output must be strictly valid JSON.
- Ensure timestamps are within the last 30 days.
**Acceptance Criteria:**
Script successfully outputs a 100-item JSON array of fake emails.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 2: Categorization Prompts [Easy]
**System Prompt:** You are an expert AI/Data Dev building InboxOS, a next-generation open-source AI
email operating system.↪
**Stack:** JSON, Markdown
**Task:** Draft LLM Prompt Templates
**Step-by-Step Instructions:**
## 1. Create `prompts/classification.json`.
- Write 3 versions of a system prompt designed to categorize an email.
- Version A: Strict rules. Version B: Few-shot examples. Version C: Chain of thought.
**Design/Architecture Constraints:**
- Prompts must instruct the LLM to output ONLY JSON.
Generated by Kimi.ai

- Keep prompts concise.
**Acceptance Criteria:**
Three distinct prompt templates are saved in the project.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 3: Keyword Filter [Medium]
**System Prompt:** You are an expert AI/Data Dev building InboxOS, a next-generation open-source AI
email operating system.↪
**Stack:** Node.js
**Task:** Implement a fast heuristic filter
**Step-by-Step Instructions:**
- Create `KeywordFilter.ts`.
- Define a list of known spam/newsletter keywords.
- Write a function that scans the email body text and returns a category if a threshold of keywords is
hit.↪
- Use this before calling the expensive LLM API.
**Design/Architecture Constraints:**
- Use Regex for fast text scanning.
- Must be case-insensitive.
**Acceptance Criteria:**
Function correctly identifies an 'unsubscribe' email as a Newsletter without using the LLM.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 4: RAG Dataset [Easy]
**System Prompt:** You are an expert AI/Data Dev building InboxOS, a next-generation open-source AI
email operating system.↪
**Stack:** Python, CSV
**Task:** Build an evaluation dataset
**Step-by-Step Instructions:**
- Create a script that generates a series of emails that form a cohesive 'Project X' conversation.
- Save them to a CSV.
- Include a 'Query' column with questions about the thread, and a 'Ground Truth' column with the
expected answer.↪
**Design/Architecture Constraints:**
Generated by Kimi.ai

- The conversation must have overlapping context to properly test RAG retrieval.
**Acceptance Criteria:**
CSV file is generated and formatted correctly for evaluation tools.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 5: Analyze Latency [Medium]
**System Prompt:** You are an expert AI/Data Dev building InboxOS, a next-generation open-source AI
email operating system.↪
**Stack:** Python, Pandas
**Task:** Analyze API performance
**Step-by-Step Instructions:**
## 1. Create `analyze_logs.py`.
- Read a provided JSONL file of API latency logs.
- Calculate the average, median, 90th percentile, and 99th percentile response times.
- Print a formatted summary to the console.
**Design/Architecture Constraints:**
- Handle potential missing data gracefully.
- Use Pandas for efficient calculation.
**Acceptance Criteria:**
Script outputs accurate percentiles from the log file.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
## B4 — Beginner Integrations
Prompt 1: Test Gmail OAuth [Easy]
**System Prompt:** You are an expert Integrations Dev building InboxOS, a next-generation open-source AI
email operating system.↪
**Stack:** Node.js
**Task:** Standalone OAuth test script
**Step-by-Step Instructions:**
- Write a script `test_oauth.js`.
- Use the `googleapis` library to generate an auth URL.
- Print it to the console, prompt the user to paste the resulting code.
- Exchange the code for a token and print the token.
Generated by Kimi.ai

**Design/Architecture Constraints:**
- Do not hardcode credentials in source control; use `.env`.
**Acceptance Criteria:**
Script generates a valid URL and successfully exchanges a pasted code for tokens.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 2: Discord Webhook [Easy]
**System Prompt:** You are an expert Integrations Dev building InboxOS, a next-generation open-source AI
email operating system.↪
**Stack:** Node.js, Axios
**Task:** Send notifications to Discord
**Step-by-Step Instructions:**
- Create `DiscordNotifier.ts`.
- Take an Email object.
- Format a Discord 'embed' JSON payload containing the email subject and AI category.
- POST it to a Discord Webhook URL.
**Design/Architecture Constraints:**
- Use proper error handling if the POST request fails.
**Acceptance Criteria:**
Function successfully sends an embedded message to a test Discord channel.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 3: Slack Notification [Medium]
**System Prompt:** You are an expert Integrations Dev building InboxOS, a next-generation open-source AI
email operating system.↪
**Stack:** Node.js, Axios
**Task:** Send notifications to Slack
**Step-by-Step Instructions:**
- Create `SlackNotifier.ts`.
- Use Slack's Block Kit format to create a nicely formatted message showing the email summary.
- Send it via HTTP POST to a Slack webhook.
**Design/Architecture Constraints:**
- Ensure the message clearly indicates it came from InboxOS AI.
Generated by Kimi.ai

**Acceptance Criteria:**
Function successfully sends a Block Kit formatted message to Slack.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 4: Fetch Gravatar [Easy]
**System Prompt:** You are an expert Integrations Dev building InboxOS, a next-generation open-source AI
email operating system.↪
**Stack:** TypeScript, Crypto
**Task:** Get user avatars
**Step-by-Step Instructions:**
- Write `getGravatarUrl(email: string): string`.
- Trim whitespace, lowercase the email.
- Hash it using MD5.
## 4. Return `https://www.gravatar.com/avatar/{hash}?d=identicon`.
**Design/Architecture Constraints:**
- The MD5 hash must be correctly hex-encoded.
**Acceptance Criteria:**
Function returns the correct standard Gravatar URL for any given email address.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 5: Parse Attachments [Medium]
**System Prompt:** You are an expert Integrations Dev building InboxOS, a next-generation open-source AI
email operating system.↪
**Stack:** Node.js, mailparser
**Task:** Extract attachment metadata
**Step-by-Step Instructions:**
- Write a script that uses `mailparser`.
- Load a raw `.eml` file from disk.
- Parse it and map over `attachments`.
- Log the filename, content type, and byte size of each attachment.
**Design/Architecture Constraints:**
- Handle emails with zero attachments gracefully.
Generated by Kimi.ai

**Acceptance Criteria:**
Script successfully outputs metadata for all attachments in an EML file.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
B5 — Beginner Security/QA
Prompt 1: Setup ESLint [Easy]
**System Prompt:** You are an expert QA/Security Dev building InboxOS, a next-generation open-source AI
email operating system.↪
**Stack:** ESLint, Prettier
**Task:** Initialize code linting
**Step-by-Step Instructions:**
- Install ESLint and Prettier plugins for TypeScript.
- Create `.eslintrc.js` and `.prettierrc`.
- Add npm scripts `lint` and `format`.
- Configure rules to enforce single quotes and semi-colons.
**Design/Architecture Constraints:**
- There must be no conflicting rules between Prettier and ESLint.
**Acceptance Criteria:**
Running `npm run lint` successfully checks the codebase without crashing.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 2: API Unit Tests [Medium]
**System Prompt:** You are an expert QA/Security Dev building InboxOS, a next-generation open-source AI
email operating system.↪
**Stack:** Jest, Supertest
**Task:** Write backend unit tests
**Step-by-Step Instructions:**
- Install `jest` and `supertest`.
- Write tests for `GET /api/health`.
- Write tests for `GET /api/users/profile`, mocking the Prisma database client so it doesn't need a
real DB connection.↪
**Design/Architecture Constraints:**
- Achieve 100% test coverage on these specific endpoint files.
Generated by Kimi.ai

**Acceptance Criteria:**
Tests pass and the coverage report indicates 100% statement coverage.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 3: Cypress E2E [Medium]
**System Prompt:** You are an expert QA/Security Dev building InboxOS, a next-generation open-source AI
email operating system.↪
**Stack:** Cypress
**Task:** Write basic E2E test
**Step-by-Step Instructions:**
## 1. Install Cypress.
- Write a test that visits the `/login` page.
- Find the email and password inputs and type mock data.
- Click the submit button.
- Assert that the URL changes to `/dashboard`.
**Design/Architecture Constraints:**
- Tests must run reliably against a locally running frontend instance.
**Acceptance Criteria:**
Cypress test runs and passes in headless mode.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 4: Check Vulnerabilities [Easy]
**System Prompt:** You are an expert QA/Security Dev building InboxOS, a next-generation open-source AI
email operating system.↪
**Stack:** npm, GitHub Actions
**Task:** Enforce dependency security
**Step-by-Step Instructions:**
- Add a step to the existing CI workflow.
- Run `npm audit --audit-level=high`.
- If it returns an error code, the GitHub Action must fail.
**Design/Architecture Constraints:**
- Do not fail the build for 'low' or 'moderate' vulnerabilities.
**Acceptance Criteria:**
Generated by Kimi.ai

CI pipeline correctly fails if a high severity vulnerability is introduced.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 5: Password Strength [Easy]
**System Prompt:** You are an expert QA/Security Dev building InboxOS, a next-generation open-source AI
email operating system.↪
**Stack:** TypeScript, Jest
**Task:** Implement password rules
**Step-by-Step Instructions:**
- Write `checkPasswordStrength(password)` returning a score 0-5.
- Score points for: length &gt; 8, uppercase, lowercase, number, special char.
- Write Jest unit tests covering boundary cases (e.g., all lowercase).
**Design/Architecture Constraints:**
- The function must be pure and synchronous.
**Acceptance Criteria:**
All unit tests for the password strength checker pass accurately.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
B6 — Beginner Docs/DevEx
Prompt 1: Local Setup Guide [Easy]
**System Prompt:** You are an expert Docs/DevEx Dev building InboxOS, a next-generation open-source AI
email operating system.↪
**Stack:** Markdown
**Task:** Document onboarding steps
**Step-by-Step Instructions:**
- Create `LOCAL_SETUP.md`.
- Document prerequisites (Node, Postgres, Docker).
- List step-by-step commands to clone, install, migrate DB, and start the app.
- Explain how to configure the `.env` file using the `.env.example`.
**Design/Architecture Constraints:**
- Use markdown code blocks for all terminal commands.
- Write in a clear, beginner-friendly tone.
**Acceptance Criteria:**
Generated by Kimi.ai

A new developer can start the app by following only this document.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 2: Database Schema Docs [Easy]
**System Prompt:** You are an expert Docs/DevEx Dev building InboxOS, a next-generation open-source AI
email operating system.↪
**Stack:** Markdown
**Task:** Document the data models
**Step-by-Step Instructions:**
- Read the `schema.prisma` file.
- Create `SCHEMA.md`.
- Create a Markdown table for each model (User, Email, Thread, etc.) listing the column name, type, and
description.↪
- Document relationships.
**Design/Architecture Constraints:**
- Use Mermaid.js syntax to create a visual ER diagram inside the markdown.
**Acceptance Criteria:**
The document accurately reflects all tables and the Mermaid diagram renders correctly.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 3: Postman Collection [Medium]
**System Prompt:** You are an expert Docs/DevEx Dev building InboxOS, a next-generation open-source AI
email operating system.↪
**Stack:** JSON, REST
**Task:** Create API interaction tools
**Step-by-Step Instructions:**
- Open Postman or Insomnia.
- Create a collection named 'InboxOS API'.
- Add requests for Login, Register, Get Profile, Get Emails.
- Include example JSON request bodies.
- Export the collection as a JSON file and save it in the repo.
**Design/Architecture Constraints:**
- Use environment variables in Postman for the `{{baseUrl}}` and `{{jwt_token}}`.
Generated by Kimi.ai

**Acceptance Criteria:**
Importing the JSON file into a fresh Postman instance yields fully configured requests.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 4: Contribution Guidelines [Easy]
**System Prompt:** You are an expert Docs/DevEx Dev building InboxOS, a next-generation open-source AI
email operating system.↪
**Stack:** Markdown
**Task:** Standardize open-source workflow
**Step-by-Step Instructions:**
- Create `CONTRIBUTING.md`.
- Define the Git branch naming convention (e.g., `feat/issue-123`).
- Define the Commit message format (Conventional Commits).
- Detail the Pull Request review process.
**Design/Architecture Constraints:**
- Be encouraging to first-time contributors.
**Acceptance Criteria:**
The contributing guide is comprehensive and formatted nicely.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 5: Git Workflow Diagram [Easy]
**System Prompt:** You are an expert Docs/DevEx Dev building InboxOS, a next-generation open-source AI
email operating system.↪
**Stack:** Mermaid.js, Markdown
**Task:** Visualize the branching strategy
**Step-by-Step Instructions:**
- Create a document `WORKFLOW.md`.
- Write a Mermaid `gitGraph` diagram showing the `main`, `develop`, and `feature/` branches.
- Show how features are merged into develop, and develop is tagged into main.
**Design/Architecture Constraints:**
- Ensure the Mermaid syntax is strictly correct.
**Acceptance Criteria:**
The markdown file renders the Git graph successfully on GitHub/GitLab.
Generated by Kimi.ai

**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
## B7 — Beginner Performance
Prompt 1: Optimize React Renders [Medium]
**System Prompt:** You are an expert Performance Dev building InboxOS, a next-generation open-source AI
email operating system.↪
**Stack:** React
**Task:** Prevent unnecessary UI renders
**Step-by-Step Instructions:**
- Analyze the `EmailList` and `EmailRow` components.
- Wrap `EmailRow` in `React.memo`.
- Use `useCallback` for click handler functions passed from parent to child.
- Use `useMemo` for derived data (like formatting dates).
**Design/Architecture Constraints:**
- Do not break existing functionality.
- Only memoize where there is a demonstrable performance benefit.
**Acceptance Criteria:**
The React Profiler shows significantly reduced render times when scrolling the email list.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 2: Database Indexes [Easy]
**System Prompt:** You are an expert Performance Dev building InboxOS, a next-generation open-source AI
email operating system.↪
**Stack:** PostgreSQL, Prisma
**Task:** Speed up DB queries
**Step-by-Step Instructions:**
## 1. Open `schema.prisma`.
- Add indexes (`@@index`) to the `Email` model on columns frequently used in WHERE clauses: `userId`,
`threadId`, and `createdAt`.↪
- Generate a new migration script.
**Design/Architecture Constraints:**
- Do not over-index; only add indexes that support the core dashboard queries.
**Acceptance Criteria:**
Generated by Kimi.ai

Running the migration applies the indexes, and `EXPLAIN ANALYZE` shows reduced query times.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 3: Redis Caching [Medium]
**System Prompt:** You are an expert Performance Dev building InboxOS, a next-generation open-source AI
email operating system.↪
**Stack:** Node.js, Redis
**Task:** Cache user profiles
**Step-by-Step Instructions:**
- Modify `GET /api/users/profile`.
- Before querying Prisma, check Redis for the key `user:profile:{id}`.
- If found, return it immediately.
- If not found, fetch from Prisma, stringify the JSON, and save to Redis with a TTL of 300 seconds (5
mins).↪
**Design/Architecture Constraints:**
- Handle Redis connection failures by falling back directly to the database.
**Acceptance Criteria:**
The endpoint is significantly faster on subsequent requests, and changes in the DB update the cache
eventually.↪
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 4: Lighthouse Audit [Easy]
**System Prompt:** You are an expert Performance Dev building InboxOS, a next-generation open-source AI
email operating system.↪
**Stack:** Lighthouse, Markdown
**Task:** Measure frontend performance
**Step-by-Step Instructions:**
- Run a production build of the Vite React app.
- Serve it locally using `npx serve`.
- Run a Chrome Lighthouse audit.
- Save the results and create a GitHub Issue detailing the top 3 recommendations to improve the score.
**Design/Architecture Constraints:**
- Test in an incognito window without extensions.
**Acceptance Criteria:**
Generated by Kimi.ai

A detailed performance report is documented with actionable improvements.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 5: Minify Static Assets [Easy]
**System Prompt:** You are an expert Performance Dev building InboxOS, a next-generation open-source AI
email operating system.↪
**Stack:** Vite, Rollup
**Task:** Optimize build sizes
**Step-by-Step Instructions:**
## 1. Open `vite.config.ts`.
- Ensure `build.minify` is set correctly.
- Add configuration to split vendor chunks (e.g., move React and React DOM into a separate vendor.js
file) to improve browser caching.↪
**Design/Architecture Constraints:**
- Ensure the production build runs without missing module errors.
**Acceptance Criteria:**
The build outputs distinct, minified vendor and application JavaScript files.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
B8 — Beginner Mobile UI
Prompt 1: Mobile Sidebar [Easy]
**System Prompt:** You are an expert Mobile UI Dev building InboxOS, a next-generation open-source AI
email operating system.↪
**Stack:** React, TailwindCSS
**Task:** Adapt layout for phones
**Step-by-Step Instructions:**
- Update the `Layout` component.
- On screens `&lt; 768px`, hide the Sidebar by using Tailwind's `hidden md:flex`.
- Add a hamburger menu icon to the top header.
- Clicking the icon should toggle a state that overlays the sidebar across the full screen with a close
button.↪
**Design/Architecture Constraints:**
- Sidebar transition must be smooth using CSS transforms.
Generated by Kimi.ai

**Acceptance Criteria:**
On a mobile viewport, the sidebar hides and can be toggled via the hamburger menu.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 2: Responsive Email List [Easy]
**System Prompt:** You are an expert Mobile UI Dev building InboxOS, a next-generation open-source AI
email operating system.↪
**Stack:** TailwindCSS
**Task:** Adapt the list view for phones
**Step-by-Step Instructions:**
- Update the `EmailRow` component.
- On desktop (`md:flex-row`), show sender, subject, date in a line.
- On mobile, use `flex-col` so Sender is on top, Subject is below, and Date is top-right aligned.
**Design/Architecture Constraints:**
- Text truncation (`truncate` class) must work on both mobile and desktop.
**Acceptance Criteria:**
The email list reads cleanly on a narrow phone screen without horizontal scrolling.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 3: Mobile Compose Button [Easy]
**System Prompt:** You are an expert Mobile UI Dev building InboxOS, a next-generation open-source AI
email operating system.↪
**Stack:** React, TailwindCSS
**Task:** Create a Floating Action Button
**Step-by-Step Instructions:**
- Create a FAB component.
- Position it `fixed bottom-6 right-6`.
- Give it the primary accent background color, a shadow, and a rounded full shape (`rounded-full`).
- It should only be visible on mobile (`block md:hidden`).
**Design/Architecture Constraints:**
- Ensure it doesn't overlap the mobile navigation bar if one exists.
**Acceptance Criteria:**
A circular compose button appears in the bottom right corner only on mobile devices.
Generated by Kimi.ai

**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 4: Fix Touch Targets [Easy]
**System Prompt:** You are an expert Mobile UI Dev building InboxOS, a next-generation open-source AI
email operating system.↪
**Stack:** TailwindCSS
**Task:** Improve accessibility
**Step-by-Step Instructions:**
- Inspect all buttons and interactive icon links.
- Ensure they have at least `p-3` or `min-h-[44px] min-w-[44px]` applied to them so they are easily
tappable with a finger.↪
**Design/Architecture Constraints:**
- Do not break desktop layout aesthetics; apply padding judiciously.
**Acceptance Criteria:**
All interactive elements pass the 44x44 pixel minimum touch target rule.
**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Prompt 5: Pull to Refresh [Medium]
**System Prompt:** You are an expert Mobile UI Dev building InboxOS, a next-generation open-source AI
email operating system.↪
**Stack:** React, Touch Events
**Task:** Add native-like interactions
**Step-by-Step Instructions:**
- Add touch event listeners (`onTouchStart`, `onTouchMove`, `onTouchEnd`) to the `EmailList` container.
- Track the Y-axis drag distance when at `scrollTop === 0`.
- If pulled down past a threshold (e.g., 60px), trigger the API refetch function and show a loading
spinner at the top.↪
**Design/Architecture Constraints:**
- Provide visual feedback (resistance/spring) during the pull interaction.
**Acceptance Criteria:**
Pulling down on the list on a mobile device correctly reloads the email data.
Generated by Kimi.ai

**Output:** Execute the necessary terminal commands to initialize or modify the codebase, and write the
complete, production-ready code. Do not use placeholders (e.g., 'TODO: implement logic'). Write
fully functional code.
## ↪
## ↪
Generated by Kimi.ai